import { CastShapePipe } from './cast-shape.pipe';

describe('CastShapePipe', () => {
  it('create an instance', () => {
    const pipe = new CastShapePipe();
    expect(pipe).toBeTruthy();
  });
});
